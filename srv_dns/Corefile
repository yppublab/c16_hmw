.:53 {
  log
  errors
  # Serve local zone
  file /etc/coredns/sdelalisoft.local.db sdelalisoft.local
  # Cache responses briefly
  cache 30
  # Forward all other queries to public resolvers
  forward . 1.1.1.1 8.8.8.8
}
